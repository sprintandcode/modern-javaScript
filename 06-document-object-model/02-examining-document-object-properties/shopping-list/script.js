let output;

output = document.all;              // gives an array or collection of the DOM or page
output = document.all[1];           // you can access individual elements

output = document.body;             // display entire body

output = document.body.children;    // display children od body

output = document.URL;

output = document.characterSet;

output = document.contentType;

output = document.forms;            // you can also individual elements of form

output = document.forms[0].id; 

output = document.forms[0].method;

// There are many properties we actually won't use on a daily basis.


console.log(output);